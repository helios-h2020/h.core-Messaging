/// <reference types="node" />
import { LoggerCommon } from './LoggerCommon';
export declare class LoggerBrowser extends LoggerCommon {
    static NAME_LENGTH: number;
    constructor(module: NodeJS.Module, context?: string, destinationStream?: {
        write(msg: string): void;
    });
}
export { LoggerBrowser as Logger };
