import { EthereumAddress } from 'streamr-client-protocol';
import { StreamQueryResult } from './StreamRegistry';
import { StreamPermission, ChainPermissions } from './permission';
import { GraphQLClient } from './utils/GraphQLClient';
export interface SearchStreamsPermissionFilter {
    user: EthereumAddress;
    allOf?: StreamPermission[];
    anyOf?: StreamPermission[];
    allowPublic: boolean;
}
export declare type SearchStreamsResultItem = {
    id: string;
    userAddress: string;
    stream: StreamQueryResult;
} & ChainPermissions;
export declare function fetchSearchStreamsResultFromTheGraph(term: string | undefined, permissionFilter: SearchStreamsPermissionFilter | undefined, graphQLClient: GraphQLClient): AsyncGenerator<SearchStreamsResultItem>;
