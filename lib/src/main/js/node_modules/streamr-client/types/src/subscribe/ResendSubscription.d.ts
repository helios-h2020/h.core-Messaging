import { Subscription } from './Subscription';
export declare class ResendSubscription<T> extends Subscription<T> {
    private resends;
    private resendOptions;
    private onResentSignal;
    onResent: (callback: () => void) => void;
    private orderMessages;
    private getResent;
}
