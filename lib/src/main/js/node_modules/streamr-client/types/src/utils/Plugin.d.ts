import { AnyInstance } from './index';
/**
 * Take prototype functions from srcInstance and attach them to targetInstance while keeping them bound to srcInstance.
 */
export declare function Plugin<TargetType, SrcType extends AnyInstance, ResultType extends (TargetType & Methods<SrcType>)>(targetInstance: TargetType, srcInstance: SrcType): ResultType;
declare type MethodNames<T> = {
    [K in keyof T]: ((undefined extends T[K] ? never : T[K]) extends Function ? K : never);
}[keyof T];
export declare type Methods<T> = Pick<T, MethodNames<T>>;
export {};
