import Ethereum, { AuthConfig } from './Ethereum';
import { Context } from './utils/Context';
import { Rest } from './Rest';
export interface UserDetails {
    name: string;
    username: string;
    imageUrlSmall?: string;
    imageUrlLarge?: string;
    lastLogin?: string;
}
export interface TokenObject {
    token: string;
}
export declare class LoginEndpoints implements Context {
    private ethereum;
    private rest;
    private authConfig;
    id: string;
    debug: import("debug").Debugger;
    constructor(context: Context, ethereum: Ethereum, rest: Rest, authConfig: AuthConfig);
    getUserInfo(): Promise<UserDetails>;
}
