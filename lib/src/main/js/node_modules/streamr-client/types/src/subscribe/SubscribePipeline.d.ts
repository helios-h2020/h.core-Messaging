/**
 * Subscription message processing pipeline
 */
import { StreamPartID } from 'streamr-client-protocol';
import { MessageStream } from './MessageStream';
import { Context } from '../utils/Context';
import { DependencyContainer } from 'tsyringe';
export default function SubscribePipeline<T = unknown>(messageStream: MessageStream<T>, streamPartId: StreamPartID, context: Context, container: DependencyContainer): MessageStream<T>;
