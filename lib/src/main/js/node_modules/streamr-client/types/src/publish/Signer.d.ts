import { StreamMessage, StreamMessageSigned, SignatureType } from 'streamr-client-protocol';
import type { AuthenticatedConfig } from '../Ethereum';
export default class Signer {
    signData: (d: string) => Promise<string>;
    constructor(authOptions: AuthenticatedConfig);
    static getSigningFunction(options: AuthenticatedConfig): (d: string) => Promise<string>;
    sign<T>(streamMessage: StreamMessage<T>, signatureType?: SignatureType): Promise<StreamMessageSigned<T>>;
}
