/**
 * Utility Types
 */
import { F } from 'ts-toolbelt';
export declare type MaybeAsync<T extends F.Function> = T | F.Promisify<T>;
export declare type StreamDefinition = string | {
    id: string;
    partition?: number;
} | {
    stream: string;
    partition?: number;
} | {
    streamId: string;
    partition?: number;
};
