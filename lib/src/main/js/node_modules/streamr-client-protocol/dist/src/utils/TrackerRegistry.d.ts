import { StreamPartID } from "./StreamPartID";
export declare type SmartContractRecord = {
    id: string;
    http: string;
    ws: string;
};
export declare type TrackerInfo = SmartContractRecord | string;
export declare class TrackerRegistry<T extends TrackerInfo> {
    private readonly records;
    constructor(records: T[]);
    getTracker(streamPartId: StreamPartID): T;
    getAllTrackers(): T[];
}
export declare function createTrackerRegistry<T extends TrackerInfo>(servers: T[]): TrackerRegistry<T>;
