import ControlMessage, { ControlMessageOptions } from '../ControlMessage';
import { StreamID } from '../../../utils/StreamID';
import { StreamPartID } from "../../../utils";
interface Options extends ControlMessageOptions {
    senderId: string;
    streamId: StreamID;
    streamPartition: number;
    accepted: boolean;
}
export default class PublishStreamConnectionResponse extends ControlMessage {
    senderId: string;
    streamId: StreamID;
    streamPartition: number;
    accepted: boolean;
    constructor({ version, requestId, senderId, streamId, streamPartition, accepted }: Options);
    getStreamPartID(): StreamPartID;
}
export {};
