import ControlMessage, { ControlMessageOptions } from '../ControlMessage';
import { StreamID } from '../../../utils/StreamID';
import { StreamPartID } from "../../../utils/StreamPartID";
interface Options extends ControlMessageOptions {
    senderId: string;
    streamId: StreamID;
    streamPartition: number;
}
export default class PublishStreamConnectionRequest extends ControlMessage {
    senderId: string;
    streamId: StreamID;
    streamPartition: number;
    constructor({ version, requestId, senderId, streamId, streamPartition }: Options);
    getStreamPartID(): StreamPartID;
}
export {};
