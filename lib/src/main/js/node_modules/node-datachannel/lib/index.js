const os = require("os");
const bindir = "../build/Release/" + os.platform() + "-" + os.arch() + "/";
const nodeDataChannel = require(bindir + "node_datachannel.node");
module.exports = nodeDataChannel;
